# 结束前对抗式复核

本复核由主Agent重新按反证清单检查，并以独立代码和记录回放验证；没有声称另有独立人工评审或子Agent评审。

| 质疑 | 检查/反例 | 结论 |
|---|---|---|
| 是否把隐藏源坐标/半径/误差模式喂给策略？ | 策略无cases/simulator/fixtures文件导入，无文件读取、反射或transport访问；PublicClient没有evaluation入口；3个困难案例仅用公开响应重放，无场景对象，动作和指标完全一致 | 静态检查与实际回放支持未读真值；不是对恶意任意代码的安全沙箱证明 |
| 同点检测是否反复随机以求平均？ | 固定坐标和频道哈希误差；四种确定性误差场，带符号零规范化；策略每频道维护已测点集合；全动作审计检查重复位置方向一致 | 没有重采误差；不同算法在不同位置取到不同偏差是相同空间误差函数的正常结果 |
| 圆域近似会不会剔除真源？ | 初始圆使用外切支持半平面，全部feasible_update检查源在保守多边形内；600组极端±1.005度测试 | 实数包含命题已证明，浮点边界未形式化 |
| 大交角不是题面强制条件吗？ | 原PDF与两份DOCX核对，附件公式另用Pandoc提取；20度来自旧实现配置，不是题面约束 | 允许直接采用集合逼近，无重大题意歧义需要暂停 |
| 接收保证是否偷用了真实R？ | 只检查候选集合全部顶点到实际点小于公开下界1000米；模拟器在行动后独立核验 | 未把1500上界当真实接收半径 |
| 光学点是否只是猜中？ | 最终候选每次清除由near或整体20米覆盖圆支撑；960局失败光学0、网格回退0；模型矛盾测试返回incomplete | 实验中没有依赖碰运气清除；通用回退保留 |
| 发现10个或16个后是否假完成？ | 晚频道20、数量下界、16个不同成功清除以及逐频道七站阴性证书；所有源由后验评估独立清点 | 发现10不退出；发现16只停止未知扫描，仍清除全部已知 |
| R10新停止证书是不是不可靠？ | Voronoi极值枚举与独立方格整格覆盖审计；移去任一1125米七站点不错误通过 | R10数学思路可行，但开发完全不改变动作，最终不包含 |
| 局部路线最优是否被偷换成全局时间最优？ | R1、R2、R7、R8完整流程负结果全部保留；最坏聚集/径向单例明显退化 | 不声称最优或逐例支配，整体门槛而非局部代理是采纳依据 |
| 是否通过最终验证不断挑赢家？ | 原开发、结构开发、简化修订、validation_lock写入、最终种子生成时间和SHA；最终前选定GATED_TOUR_MULTI_PARALLAX | 验证后没有更换候选或修改运行源码 |
| 失败是否被丢弃？ | 15272局矩阵全部计入，失败惩罚规则固定360000秒，完整目录/日志均保留 | 性能矩阵失败0；3类注入故障测试预期失败、首次HTTP沙箱pre-enter失败单独报告，不伪装性能成功 |
| 统计独立性是否夸大？ | 最终120布局，每布局4误差×2舍入；以布局成块并按6结构分层bootstrap | 960次策略成功不宣称960个独立布局；生成器不是官方分布 |
| 新机制是不是都必要？ | R9边际不足1%、R10零、删除R3目标再观测反而更好；最终选择简化版 | 不把实现数量作为创新成就；R10和额外再观测未纳入最终候选 |
| 为什么不针对93.63%退化再修补？ | 这是锁定后的最终验证反例 | 保留反例并结束，避免验证集变成隐蔽开发集。建议只按本次均值/P95目标替换，不承诺逐例不退化 |
| 是否触碰了当前推荐或官方测试？ | 87项冻结源/题面材料哈希保持、104条旧基线精确重现；本机自建随机端口服务器已关闭 | 推荐未覆盖，官方演练0、正式0、无云端推送 |

复核证据：adversarial_tests_selected.log、public_transcript_replay.json、self_http_check.json、validation_lock.json、validation_generation.json、FINAL_EVIDENCE.json、final_paired.csv。初次本机HTTP及图形依赖缺失/下载受限的基础设施失败日志保留；它们没有启动策略场景，不混入清除成功率分母。
