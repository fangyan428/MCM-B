# 最终数据生成前的简化候选修订

触发证据来自开发消融，而不是最终验证：删除“切换任务时再原地测目标一次”的GATED_TOUR_MULTI_PARALLAX，在原开发主批3314.49秒，优于带该步骤的3328.16秒；两者P95分别3710.09、3705.76秒。原地再观测并非必需贡献，不能只因已经实现就强行保留。

在最终锁及最终案例生成之前，将这个删除步骤的版本加入选择集合，并补跑同一60布局结构开发的两种舍入。依旧要求四批各自通过基线5%/成功/P95门槛，并用四批相对均值等权选择，不改变阈值、场景、误差场或任何数值参数。平局优先此简化版本，再按原复杂度顺序。

本修订明确增加了一个概念消融候选的开发评估预算；不会伪称它在原选择名单内，也不把它算新机制轮。所有原版本与消融结果继续保留。最终数据尚未生成，验证后禁止再切换候选的规则不变。
