# 第三问调度补充实验：事前方案

2026-09-13，用户授权针对当前算法的不足做优化。原 `innovation` 策略、配置及HTTP入口均保持冻结；本次只在独立目录改变任务排序，不改变测站、频道扫描、几何更新、接收检查、清除或停止条件。

## 候选与主对照

主对照为论文当前采用的 `GATED_TOUR_MULTI_PARALLAX`，记为 CURRENT，不是更早的 round19 BOTH。

- COVERAGE_ORDER：保持原调度选择搜索或服务的决定，仅用未知频道剩余区域的保证覆盖面积与行动费用重排搜索站。
- COVERAGE_PRIORITY：进一步比较估计发现数与已知源服务的单位时间收益。
- SERVICE_PAIR：用真实第一行动点、服务结束位置代理及检测/清除费用，对两项任务作短期规划。
- SERVICE_TOUR：沿用完整滚动路线，加入上述服务代价及位置代理。

面积均匀、乐观未知源数和服务时间均仅是调度近似，不是题目新增条件，不参与位置剔除或退出。候选只有上述四种，不进行数值参数扫描。旧最终慢例已知，仅用于解释，不进入新的开发/验证汇总。

## 开发与验证

生成六类场景：均匀、外缘、聚集、最小接收半径、窄径向、接收边界；每类显式覆盖源数10至16。

- 开发：每个类别与源数组合1个布局，共42个新布局；各4种固定空间误差与2种舍入，每方案336次运行。
- 选择：候选须开发全部完成并通过独立动作审计，两种舍入平均总时间均降低、P95与最大总时间均不升。合格者按两种舍入的平均相对时间最小者预选；并列按候选名排序。若无人合格，本轮结束，不强行替换。
- 预锁：选择后冻结所有相关源码、配置、生成器及规则，再生成新验证布局，不在验证结果出现后改选。
- 验证：每个类别与源数组合2个布局，共84个独立新布局，4种固定误差与2种舍入；仅比较CURRENT与预选候选，每方案672次运行。
- 采用门槛：两种舍入平均总时间分别至少减少5%，P95及最大总时间均不升，逐局每源平均时间改善；所有运行完成，接受动作、计时、位置保留和停止证书审计全部通过。各类别、源数、误差和最坏单例必须另报，不能删除退化结果。达不到则保留当前正式候选。

开发种子从2026091300开始，验证种子从2026092300开始；生成前检查种子区间不重叠。同一物理布局下四种误差及两种舍入不算独立布局；置信区间以布局为块计算。所有失败保留，失败计时惩罚至少360000秒。

运行顺序轮换，完整原始日志仅保留本地 `question3/results/dispatch_20260913`，Git保存协议、源码、配置、fixture、源码锁、汇总和报告。仅运行自建环境，不启动官方测试。本轮预计约20分钟，证据不足则结束并报告，不临时扩大搜索。
