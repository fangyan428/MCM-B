# R4 多目标连续集合共享观测

独立核心机制：一次主目标移动测向后的实际位置，也服务其他已知未清目标；对每个集合证明当前点对所有候选源距离均小于1000米才测。新测量不断收缩目标集合，不限于为另一频道缓存一次满足20度交角的第二测量。每个点每个频道最多一次，合法的 near 立即清除并计费。

SHARED=R1+R3+多目标共享；SHARED_PARALLAX再加R2。REFRESH与REFRESH_PARALLAX为关掉多目标共享的对应消融。不调整共享额度或顺序，按频道遍历全部符合接收证书的目标；额外测量浪费作为失败机制保留。
